apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: exampleapp-ingress
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: exampleapp-dapr
              servicePort: 80
            path: /